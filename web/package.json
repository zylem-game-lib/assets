{
	"name": "@zylem/assets-uploader-web",
	"private": true,
	"version": "0.1.0",
	"description": "SolidJS UI for batch-uploading directories of assets to the zylem-assets Go server.",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "tsc -b && vite build",
		"preview": "vite preview",
		"typecheck": "tsc -b --noEmit"
	},
	"dependencies": {
		"solid-js": "^1.9.3"
	},
	"devDependencies": {
		"@tailwindcss/vite": "^4.0.0",
		"tailwindcss": "^4.0.0",
		"typescript": "^5.6.3",
		"vite": "^6.0.3",
		"vite-plugin-solid": "^2.11.0"
	}
}
